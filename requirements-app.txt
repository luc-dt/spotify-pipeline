# ========================================================================================
# Spotify Music Intelligence Platform - Streamlit Runtime Dependencies
# ========================================================================================
# Purpose:
#   Minimal, pinned dependency set for the Streamlit container only.
#   - Excludes PySpark (sdist-only distribution, incompatible with --only-binary :all:,
#     and useless without a JVM in the UI container).
#   - All packages ship binary wheels so the image installs with --only-binary :all:.
#     (satisfies SonarCloud docker:S8541/S8544)
# ========================================================================================

streamlit==1.62.0
duckdb==1.5.5
pandas==2.3.3
pyarrow==25.0.1
plotly==7.0.0
python-dotenv==1.2.2
