[pytest]
testpaths = tests
norecursedirs = airflow .venv data .git .agents .pytest_cache
pythonpath = .
# Default timeout: 120s for all tests (prevents infinite hangs on Windows CI)
timeout = 120
# Mark tests that may take longer (PySpark tests need more time)
addopts = --tb=short
